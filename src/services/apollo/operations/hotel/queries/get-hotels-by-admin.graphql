query GetHotelsByAdmin($options: Options) {
  hotelsByAdmin(options: $options) {
    count
    nodes {
      id
      name
      summary
      thumbnail
      city
      state
      createdAt
    }
  }
}
