query GetHotelById($id: ID!) {
  hotel(id: $id) {
    name
    summary
    description
    images
    thumbnail
    logo
    longitude
    latitude
    address
    addressNumber
    zipCode
    neighborhood
    state
    city
  }
}
