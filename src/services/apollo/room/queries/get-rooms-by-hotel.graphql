query GetRoomsByHotel($hotelId: ID!, $options: Options) {
  roomsByHotel(hotel: $hotelId, options: $options) {
    count
    nodes {
      id
      name
      summary
      thumbnail
      createdAt
    }
  }
}
